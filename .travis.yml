language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-kendo-mobile' ]; then cd .. && eval "mv $currentfolder generator-kendo-mobile" && cd generator-kendo-mobile; fi

